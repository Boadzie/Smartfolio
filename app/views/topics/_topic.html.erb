<div class="row">
    <div class="col-md-9">
        <div class="card mb-4 box-shadow" >
        
        <div class="card-body">
            <p class="card-text">
            <span ><%= link_to topic.title, topic_path(topic) %></span>
            </p>
            
            <div class="d-flex justify-content-between align-items-center">
            </div>
        </div>
        </div>
    </div>
    <div class="4">
       <%= link_to 'Delete', topic_path(topic), method: :delete, data:{confirm:"Are you sure?"}, class:"btn btn-danger" %>
       <%= link_to 'Edit', edit_topic_path(topic), class:"btn btn-dark" %>
    </div>
</div>

<!--, class:"card-img-top" -->