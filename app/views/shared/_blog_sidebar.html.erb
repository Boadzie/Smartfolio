<aside class=" blog-sidebar">
    <div class="p-3 mb-3 bg-light rounded">
      <h4 class="font-italic">Why Computer Science?</h4>
      <hr>
      <p class="mb-0">Computer Science <em>is a passion</em> that I can't leave without. Using it as medium for innotion and growth is always a delight.
    </p>
    </div>
    <div>
       <%= source_helper("alert alert-info") %>
    </div>
     <div class="p-3">
        <h1>Topics</h1>
          <% @side_bar_topic.each do |topic| %>
          <p><%= link_to topic.title, topic_path(topic) %></p>
        <% end %>
     </div>
    <div class="p-3">
      <h4 class="font-italic">Elsewhere</h4>
      <hr>
      <ul class="list-unstyled">
        <li><a href="#"><%= fa_icon "github 3x"%></a></li>
        <li><a href="#"><%= fa_icon "instagram 3x"%></a></li>
        <li><a href="#"><%= fa_icon "facebook 3x"%></a></li>
        <li><a href="#"><%= fa_icon "twitter 3x"%></a></li>
      </ul>
    </div>
</aside><!-- /.blog-sidebar -->