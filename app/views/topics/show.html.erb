<main role="main">
      <div class="row">
        <div class="col-md-12 blog-main">
          <h1><%= @topic.title%></h1>
          <%= paginate @blogs %>
          <%= render @blogs %>
           <span><%= link_to 'Back', topics_path, class:"btn btn-dark btn-block" %></span>
          <nav class="blog-pagination">
            <%= paginate @blogs %>
          </nav>
        </div><!-- /.blog-main -->
      </div><!-- /.row -->

</main><!-- /.container -->

